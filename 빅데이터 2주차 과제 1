a = [2, 3, 4, 5, 6]
new_list = []

# 리스트의 끝에서부터 pop하여 새로운 리스트에 추가하여 역순으로 만듦
for _ in range(len(a)):
    new_list.append(a.pop())

# 새로운 리스트를 기존 리스트에 복사
a[:] = new_list

print(a)  # 결과 출력: [6, 5, 4, 3, 2]
